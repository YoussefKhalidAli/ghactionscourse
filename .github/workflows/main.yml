name: test triggers

on:
  push:
    branches: main

jobs:
  example-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print welcome message
        run: echo "welocme to first job"

      - name: Checkout Coded
        uses: actions/checkout@v6.0.1

      - name: List files
        run: ls -la
